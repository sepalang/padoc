import animals from './index.js';

describe('test', () => {
  
  it('animals', () => {
    expect(typeof animals).toEqual("object");
  });
  
  it('minimals', () => {
    expect(typeof animals).toEqual("object");
  });
  
  it('decimals', () => {
    expect(typeof animals).toEqual("object");
  });
  
});
